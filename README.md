# Proyecto Vercel + Docker

Este proyecto demuestra cómo usar Docker para compilar localmente y subir a Vercel con `vercel deploy --prebuilt`.

Incluye salida precompilada en `.vercel/output` como espera Vercel.
